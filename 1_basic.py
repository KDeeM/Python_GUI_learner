# import tkinter module
import tkinter as tk

# define a window object that holds the program
window = tk.Tk()

# starts an infinite loop that prevents the program from shutting itself down
# the program closes when this loop is closed or encounters an error
window.mainloop()